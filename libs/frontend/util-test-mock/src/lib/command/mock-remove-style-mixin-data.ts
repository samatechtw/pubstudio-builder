import { IRemoveStyleMixinData } from '@pubstudio/shared/type-command-data'
import { IStyle } from '@pubstudio/shared/type-site'

export const mockRemoveStyleMixinData = (style: IStyle): IRemoveStyleMixinData => {
  const data: IRemoveStyleMixinData = style
  return data
}
